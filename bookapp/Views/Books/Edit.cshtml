@using Book_Dal

@model Book
@{
    ViewData["Title"] = "Edit Book";
}

<h1>Edit Book</h1>

<form asp-action="Edit" method="post">
    <input type="hidden" asp-for="Id" />

    <div class="form-group">
        <label for="Title">Title</label>
        <input type="text" class="form-control" id="Title" asp-for="Title" required />
    </div>

    @* <div class="form-group">
        <label for="AuthorName">AuthorName</label>
        <input type="text" class="form-control" id="AuthorName" asp-for="AuthorName" required />
    </div> *@
    @* <div class="form-group">
        <label asp-for="AuthorId">Author</label>
        <select asp-for="AuthorId" class="form-control" asp-items="@(new SelectList(ViewBag.Authors, "AuthorId", "Name"))"></select>
        <span asp-validation-for="AuthorId" class="text-danger"></span>
    </div> *@
    @* <div class="form-group">
        <label for="authorId">Select Author</label>
        <select id="authorId" name="AuthorId" class="form-control">
            @foreach (var author in ViewBag.Authors as SelectList)
            {
                <option value="@author.Value" @(author.Value == Model.AuthorId.ToString() ? "selected" : "")>
                    @author.Text
                </option>
            }
        </select>
    </div> *@
    <div class="form-group">
        <label for="AuthorId">Select Author</label>
        <select id="AuthorId" name="AuthorId" class="form-control">
            @foreach (var author in ViewBag.Authors as SelectList)
            {
                <option value="@author.Value" selected="@(author.Value == Model.AuthorId.ToString() ? "selected" : null)">
                    @author.Text
                </option>
            }
        </select>
    </div>

    <div class="form-group">
        <label for="YearPublished">Year Published</label>
        <input type="number" class="form-control" id="YearPublished" asp-for="YearPublished" required />
    </div>

    <button type="submit" class="btn btn-primary">Save Changes</button>
</form>

<a href="@Url.Action("Index")" class="btn btn-secondary">Back to List</a>